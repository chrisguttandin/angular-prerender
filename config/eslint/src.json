{
    "env": {
        "node": true
    },
    "extends": "eslint-config-holy-grail",
    "overrides": [
        {
            "files": ["*.ts"],
            "rules": {
                "node/file-extension-in-import": ["error", "always"]
            }
        }
    ]
}
